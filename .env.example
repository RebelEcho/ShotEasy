# 部署目标：vercel（默认）或 cloudflare
# Vercel 部署时不需要设置，默认就是 vercel
# Cloudflare 部署时在 Dashboard → Settings → Environment Variables 设置：
#   VITE_DEPLOY_TARGET = cloudflare
# 同时在 Cloudflare Pages 构建命令中设置：
#   DEPLOY_TARGET=cloudflare pnpm build
VITE_DEPLOY_TARGET=vercel
